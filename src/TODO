X Changer param out de List::get_lists()
  actuellement un array ; Ã  changer en ref sur array car sinon on ne
  peut pas gÃ©rer le cas ou il renvoie undef (transformÃ© en un tab avec la premiÃ¨re
  entrÃ©e qui est undef)

x Pb de droits pour les rep bin, doc, etc, locale, sample (root)

X Pb droit sur /home/sympa/arc (root)

o Pb de compilation des binaires C

Ajouter un HOWTO-virtualdomain qui explique les manip et fournir un
  exemple de robot.conf

x Ajouter instruct d'nstall aprÃ¨s le make nstall :
* sendmail
  + alias pour sympa, listmaster,...
  + utilisation SMRSH (configure --bindir=/etc/smrsh)
  + creation + droits sur /etc/mail/sympa_aliases (write pour sympa)
  + prise en compte du fichier d'alias par sendmail
* syslog
  + edit /etc/syslogd.conf puis restart
* apache
  + install mod fastcgi (donner URL)
  + pb ipcdir (a reporter aux auteurs de fastcgi) : double pb pour creation
    dir et subdir
  + ajout de l'alias dans conf apache
  
Pb avec Mandrake :
  o creation utilisateur sympa
  o msgfmt pas dispo par défaut (gettext à ajouter dans dépendance)
  o MHonArc::UTF8 pas installé
  o Regexp::Common pas installé (pb installé dans /usr/locale/mhonarc)
  o Pb setloate(en_US) au démarage sympa.pl
  o Unable to create required directory '/home/sympa/bounce'
  X MAILERPROGDIR pas bien initialisé
  o confusion arguments configure : --xxdir et --with-xxdir
  
o Un script à lancer pour initialiser la DB
o Pb : base et tables crées par wwsympa=======
o Traductions :
  o virer les doublons
  o avoir un nomage commun dans scénarios + pas action dépendant
  o mettre au clair
    o email/ e-mail / adresse élec
    o Failed  / impossible / erreur / échec / problème
    o autorisation
    o espaces documents / shared /
    o propriétaires / gestionnaires
    o répertoire / dossier
    o FYI : a titre informatif
    o include  / synchronize
    o diffusé / distribué
    o restricted, limited / réservé / limité /restreinte
    o abonnés / membres
    o Subject / Sujet ou objet
    o supprimé / retiré / désabonné
    o feature / action / operation
o restaurer choix langue sur page accueil
o Pb affichage to_moderate quand document à modérer


    
